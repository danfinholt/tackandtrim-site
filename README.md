# Tack & Trim Static Site v5 — Navigation Charts

This version uses the Avenir-style typography direction, the smaller header logo, the sailing photography, stronger credibility highlights, and a new “How I Think” section with navigation chart concepts.

Open `index.html` locally to preview. Upload `index.html`, `styles.css`, and the `assets` folder to your static host.
